/**
 * App — Root component
 *
 * Single-page slot machine portfolio. No router needed.
 */

import { SlotMachine } from './components/slot'

export default function App() {
  return <SlotMachine />
}
